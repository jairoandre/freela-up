box: olger/box-yeoman
build:
  steps:
    - wercker/npm-install
    - olger/yeoman-bower-install
    - wercker/grunt:
        tasks: build
  after-steps:
    - hipchat-notify:
        token: $HIPCHAT_TOKEN
        room-id: $HIPCHAT_ROOM_ID
        from-name: wercker
deploy:
  steps:
    - script:
        name: wattt
        code: pwd
    - heroku-deploy
  after-steps:
    - hipchat-notify:
        token: $HIPCHAT_TOKEN
        room-id: $HIPCHAT_ROOM_ID
        from-name: wercker
